# Vox Landing Site

Готовый одностраничный лендинг Vox на чистом HTML/CSS/JS.

## Что внутри
- адаптивная верстка;
- встроенное оптимизированное демо Vox (~2.5 MB вместо исходных ~41 MB);
- sticky navigation;
- анимации появления;
- FAQ accordion;
- рабочие CTA и формы: отправка открывает WhatsApp с заполненным сообщением;
- модальное окно запроса пилота;
- блок безопасности без неподтвержденных compliance-обещаний;
- `vercel.json` для простого деплоя на Vercel.

## Локальный запуск
Откройте `index.html` напрямую или запустите локальный сервер:

```bash
python -m http.server 8080
```

и перейдите на `http://localhost:8080`.

## Перед публикацией
1. Подтвердить финальную цену Pro (сейчас на сайте 23 900 ₸ / место / месяц как в текущем питче).
2. Подтвердить право публично использовать название AqbobekRC в блоке трекшна.
3. При необходимости заменить WhatsApp `+7 705 345 67 89` в `script.js` и ссылках в `index.html`.
4. Добавить юридические документы/политику конфиденциальности, когда они будут готовы.
